- is_about ||= false
- is_staff ||= false
- is_contact ||= false
.page-header
  %h1.project-headline
    %span
      %small= link_to 'About', '/project/about' unless is_about
      = "About" if is_about
    %span
      %small= link_to 'Staff', '/project/staff' unless is_staff
      = "Staff" if is_staff
    %span
      %small= link_to 'Contact', '/project/contact' unless is_contact
      = "Contact" if is_contact
